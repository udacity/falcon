// implement the 'larger' function that takes two Ints
// and returns the larger Int.
//
// Example Use:
// larger(x: 2, y: 1) --> 2
// larger(x: 0, y: 100) --> 100
func larger(x: Int, y: Int) -> Int {
    // replace this code
    return (x > y) ? x : y
}
